// Generated by CoffeeScript 1.7.1
var app;

app = angular.module('angular-fire-harp', ['ui.bootstrap', 'firebase']);

app.value('firebaseUrl', window.FIREBASE_URL);

app.controller('MainCtrl', function($scope, $firebase, firebaseUrl) {
  var ref;
  this.hello = 'Hello Angular';
  this.firebaseUrl = firebaseUrl + '/messages';
  ref = new Firebase(firebaseUrl);
  this.sync = $firebase(ref);
  this.messages = this.sync.$asArray();
  this.addMessage = function(newMessage) {
    this.messages.$add({
      body: this.newMessage
    });
    return this.newMessage = '';
  };
  this.deleteMessage = (function(_this) {
    return function(message) {
      return _this.messages.$remove(message);
    };
  })(this);
  this.newMessage = '';
  return void 0;
});

//# sourceMappingURL=app.map
